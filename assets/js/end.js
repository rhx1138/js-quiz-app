// const highScoresList = document.getElementById('finalScore')
// const highScores = JSON.parse(localStorage.getItem('finalScore')) || [];
// saveHighScore.innerHTML = highScores
//     .map(score => {
//         return `<li class="high-score">${score.name} - ${score.score}</li>`;
//     })
//     .join("");

// will save scores in local storage